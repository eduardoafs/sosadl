// ok: testDoExpr.iosts

library testDoExpr is {
  
  system test() is {
    
    datatype type1 is tuple{z:integer, t:integer} {
    	function (data:type1)::y(x:integer):type1 is {
    		return tuple{z=data::z+x, t=data::t+x}
    	} 
    }
    
    gate gate1 is {
      connection connection1 is in{type1}
    } guarantee {
      protocol gate1protocol is {
        do y(5)::z
      }
    }
    
    behavior test is {
      do y(5)::z
    }
  }
}