// ok

library testType7 is {
  
  datatype type1 is sequence{integer}
  
  system test() is {
    
    datatype type2 is sequence{integer}
    
    gate gate1 is {
      connection connection1 is out{type1}
    } guarantee {
      protocol gate1protocol is {
        done
      }
    }
    
    behavior test is {
      done
    }
  }
}