// ok
with Base

library testLibrary5 is {
  datatype type1 is integer {
  	function (i:type1)::add(j:type1):type1 is {
  	  return (i+j)
  	}
  }

  function (i:type1)::mult(j:integer):integer is {
  	return (i*j)
  }
}