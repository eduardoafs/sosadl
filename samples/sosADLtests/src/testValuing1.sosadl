// ok

library testValuing1 is {
  
  system test() is {
    
    datatype type1
    
    gate gate1 is {
      connection connection1 is out{type1}
    } guarantee {
      protocol gate1protocol is {
        // valuing is alone
        value data is integer = 0
      }
    }
  
    behavior test is {
      // valuing is alone
      value data is integer = 0
    }
  }
}