// ok

library testType4 is {
  
  datatype type1 is tuple{x:integer, y:integer}
  
  system test() is {
    
    datatype type2 is tuple{x:integer, y:integer}
    
    gate gate1 is {
      connection connection1 is out{type1}
    } guarantee {
      protocol gate1protocol is {
        done
      }
    }
    
    behavior test is {
      done
    }
  }
}